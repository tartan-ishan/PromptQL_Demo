{
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "ts-node index.ts serve --configuration .",
    "watch": "nodemon --exec ts-node index.ts serve --configuration ./ --pretty-print-logs"
  },
  "dependencies": {
    "@hasura/ndc-lambda-sdk": "^1.9.0",
    "@tsconfig/node20": "^20.1.4",
    "ts-node": "^10.9.2",
    "@hasura/ndc-duckduckapi": "0.5.3",
    "csv-parse": "^5.5.0",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "dotenv-cli": "^7.4.2"
  }
}
